"""Indicator and chart helper module.

The visit-level chart helpers are still kept near the Streamlit page code because
they depend directly on the active UI month-filter state. Move them here later by
passing the selected period explicitly.
"""
